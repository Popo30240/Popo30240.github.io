/*
(async () => {
    await fetchData("http://localhost:5678/api/works");
    
    // Maintenant, les données sont disponibles dans globalData
    console.log("Données accessibles globalement :", globalData);
})();
*/
// On récupérera les données de la galerie.
fetchDataGallery();